# STT-Confomer-Implementation
Implementation of an end-to-end speech-to-text model built from scratch around a Conformer encoder and a Transformer decoder. Trained on Mozilla Common Voice (FR), it investigates how convolution-augmented attention improves phonetic locality modeling compared to a standard Transformer, with beam search decoding and subtitle generation pipeline.
